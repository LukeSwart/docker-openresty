worker_processes 1;
error_log stderr notice;
daemon off;

# env DUWAMISH_GUNICORN_PORT;
# env DUWAMISH_GUNICORN_PORT_8002_TCP_ADDR; # ie '172.17.0.14'

events {
    worker_connections 1024;
}

http {

    ##
    # Basic Settings
    ##

    #sendfile on;
    #tcp_nopush on;
    #tcp_nodelay on;
    #keepalive_timeout 65;
    #types_hash_max_size 2048;
    # server_tokens off;

    # server_names_hash_bucket_size 64;
    # server_name_in_redirect off;

    # include /etc/nginx/mime.types;
    include /usr/local/openresty/nginx/conf/mime.types;
    # default_type application/octet-stream;
    charset utf-8;
    
    ##
    # Logging Settings
    ##

    error_log /var/log/nginx/error.log warn;
    access_log /var/log/nginx/access.log;

    ##
    # Gzip Settings
    ##

    # gzip on;
    # gzip_disable "msie6";

    # gzip_vary on;
    # gzip_proxied any;
    # gzip_comp_level 6;
    # gzip_buffers 16 8k;
    # gzip_http_version 1.1;
    # gzip_types text/plain text/css application/json application/x-javascript text/xml application/xml application/xml+rss text/javascript;
    
    # upstream django {
    #     server duwamish_gunicorn:8002;
    # }


    ##
    # nginx-naxsi config
    ##
    # Uncomment it if you installed nginx-naxsi
    ##

    # include /etc/nginx/naxsi_core.rules;

    ##
    # Virtual Host Configs
    #

    # include /etc/nginx/conf.d/*.conf;
    # include /etc/nginx/sites-enabled/*;
    server {
        # echo "inside the nginx server!";
        listen 80;
        server_name dev.heyduwamish.org;
        # root /home/lucas/duwamish-dev/public;

        access_log /var/log/nginx/server_access.log;
        error_log /var/log/nginx/server_error.log;

        location /static/ {
            echo "we are inside the nginx static.";
            # set_by_lua $server_location 'return os.getenv("DUWAMISH_GUNICORN_PORT_8002_TCP_ADDR")';
            
            # proxy_pass "http://" + $server_location + ":8002";
            # echo "here is our server location: $server_location";
            # set $httplocation "http://_${server_location}_:8002";
            # set $http_var http://;
            # set $port_var :8002;
            
            # echo "here is our http prefix: $http_var";
            # echo "here is our port: $port_var";
            # echo "here is our final proxy: $http_var$server_location$port_var";
            alias /duwamish/staticfiles/;
        }

        location / {
            echo "we are inside the nginx root.";
            # an HTTP header important enough to have its own Wikipedia entry:
            #   http://en.wikipedia.org/wiki/X-Forwarded-For
            # proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            # enable this if and only if you use HTTPS, this helps Rack
            # set the proper protocol for doing redirects:
            # proxy_set_header X-Forwarded-Proto https;

            # pass the Host: header from the client right along so redirects
            # can be set properly within the Rack application
            # proxy_set_header Host $http_host;

            # we don't want nginx trying to do something clever with
            # redirects, we set the Host: header above already.
            # proxy_redirect off;

            # Try to serve static files from nginx, no point in making an
            # *application* server like Unicorn/Rainbows! serve static files.
            # if (!-f $request_filename) {
            #         #      proxy_pass http://hello_app_server;
            #         proxy_pass http://django;
            #         break;
            #     }
            # set_by_lua $server_location 'return os.getenv("DUWAMISH_GUNICORN_PORT")';
            # try_files $server_location $uri @django;
            # set_by_lua $server_location 'return os.getenv("DUWAMISH_GUNICORN_PORT_8002_TCP_ADDR")';
            # # proxy_pass "http://" + $server_location + ":8002";
            # echo "here is our server location: $server_location";
            # # set $httplocation "http://_${server_location}_:8002";
            # set $http_var http://;
            # set $port_var :8002;
            
            # echo "here is our http location: $http_var";
            # # proxy_pass $httplocation;
            # proxy_pass $http_var$server_location$port_var;
            # proxy_pass http://django;
            proxy_pass http://duwamish-gunicorn:8002;
        }
        
        # location @django {
        #     proxy_pass http://django;
        # }

    }
}

# mail {
#	# See sample authentication script at:
#	# http://wiki.nginx.org/ImapAuthenticateWithApachePhpScript
# 
#	# auth_http localhost/auth.php;
#	# pop3_capabilities "TOP" "USER";
#	# imap_capabilities "IMAP4rev1" "UIDPLUS";
# 
#	server {
#		listen     localhost:110;
#		protocol   pop3;
#	}
# 
#	server {
#		listen     localhost:143;
#		protocol   imap;
#		proxy      on;
#	}
# }
